<IfModule authz_core_module>
	Require all denied
</IfModule>
<IfModule !authz_core_module>
	Deny from all
</IfModule>

<!-- <IfModule mod_rewrite.c>
	RewriteEngine On
    RewriteBase /VANTRIPPERS_CI4/public/

    # Redirect all requests to index.php if the requested resource doesn't exist
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule> -->
